# Gemini 项目规则

本仓库的项目级开发规则唯一来源是 [`AGENTS.md`](AGENTS.md)。开始工作前请完整读取并遵循该文件；其中的规则优先于本文件。

本文件不重复维护项目结构、CLI 参数、领域工作流、指标口径或产物细节。相关任务请按 `AGENTS.md` 的“领域任务”规则读取对应 `.agents/skills/*/SKILL.md`，并以代码、配置、测试、CLI `--help` 和运行产物 Manifest 为准。
