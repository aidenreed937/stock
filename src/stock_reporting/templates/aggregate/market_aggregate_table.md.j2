{% if show_header %}范围：{{ title }}（非逐标的快照）
数据源：{{ snapshot.source }} | 状态：{{ status }} | 覆盖：{{ coverage_text }} | 新鲜度：{{ freshness }}
{% endif %}| 指标 | 数值 | 说明 |
|---|---:|---|
{% for row in rows %}| {{ row.label }} | {{ row.value }} | {{ row.note }} |
{% endfor %}
